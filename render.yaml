services:
  - type: web
    name: pulse
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn asgi_app:app --host 0.0.0.0 --port ${PORT:-8000}"
