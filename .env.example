# Copy this file to `.env` and fill in real secret values for local development.
# This example intentionally contains placeholder values and should be safe to
# commit to version control.

# Database config
DB_USERNAME=postgres
DB_***REMOVED***=change_me
DB_DATABASE=pulse

# Auth / JWT settings
***REMOVED***=replace_with_a_secure_value
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30
